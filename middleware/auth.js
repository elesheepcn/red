export default function({ route, redirect, store }) {
    // console.log("window.onbeforeunload", window.onbeforeunload);
    // store.commit("tongleLoadingStatus", false);
    // const noValidPath = ["login", "login-forgetPassword", "login-register"];
    // if (!noValidPath.includes(route.name)) {
    //   clearTimeout(window.checkIsLoginTimer);
    //   window.checkIsLoginTimer = setTimeout(() => {
    //     if (!store.state.isLogin) {
    //       redirect("/login");
    //     }
    //   }, 100);
    // }
    // console.log("router", route);
  }
  